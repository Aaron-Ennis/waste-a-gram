name: wasteagram
description: An app to track and shame food waste.

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  location: ^4.3.0
  image_picker: ^0.8.3
  share_plus: ^2.1.4
  http: ^0.13.3
  intl: ^0.17.0
  firebase_core: ^1.4.0
  cloud_firestore: ^2.4.0
  firebase_storage: ^10.0.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  pedantic: ^1.10.0
  test:  

flutter:

  uses-material-design: true

  # assets:
  #   - assetsFolder/path
  # fonts:
  #   - family:
  #     fonts:
  #       - asset: fonts/Schyler-Regular.ttf
  #       - asset: fonts/Schyler-Italic.ttf
  #         style: italic
  #         weight: 
